{
  "name": "lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --watchAll=false",
    "test:logger": "ts-node tests/logger-test.ts",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run test:ci && npm run typecheck",
    "build": "tsc",
    "build:dev": "node build.js dev",
    "build:esbuild": "node build.js",
    "build:watch": "tsc -w",
    "watch": "npx nodemon",
    "watch:typecheck": "tsc --watch --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.19.1",
    "@aws-sdk/client-bedrock-runtime": "^3.799.0",
    "@aws-sdk/client-s3": "^3.802.0",
    "@family-trivia/shared": "*",
    "fuse.js": "^7.3.0",
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/jest": "^29.5.14",
    "esbuild": "^0.25.12",
    "jest": "^29.7.0",
    "nodemon": "^3.1.14",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
